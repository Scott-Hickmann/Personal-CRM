Bad content JSON. Validator says: `{{validation_error}}`

Start over. Return one whole content object for this exact ID:

{{required_manifest}}

Keep the exact ID. Follow the content schema. Return raw JSON only, without
Markdown or code fences.
